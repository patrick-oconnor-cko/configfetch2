# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
*.swp

# Local secrets — never commit tokens/keys (the CAT bearer token is short-lived
# and always pasted at run time)
*.token
.env
cat-api/cat_token
.claude/settings.local.json
# app/dev-creds.json holds the local Client ID / SK / PK used to prefill the form. Copy
# app/dev-creds.example.json to app/dev-creds.json and fill it in; it is never committed.
app/dev-creds.json
